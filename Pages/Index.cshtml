@page
@model IndexModel
@{
    ViewData["Title"] = "IBAS Cantine Menu";
}
@using IBAS.MenuApp.Model

<div class="text-center">
    <h1 class="display-4">IBAS Cantine</h1>
    <div class="row justify-content-center">
        <div class="col-auto">
            <table class="table table-responsive">
                <th>DOW</th><th>Warm dish</th><th>Cold dish</th>
                @foreach (MenuItemDTO item in Model.MenuItems)
                {
                    <tr>
                        <td>@item.DOW</td>
                        <td>@item.WarmDish</td>
                        <td>@item.ColdDish</td>
                    </tr>
                }
            </table>
        </div>
    </div>
</div>

